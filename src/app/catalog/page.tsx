import { WorkshopCatalog } from '@/views/catalog/WorkshopCatalog';

export const metadata = {
  title: 'Workshop Catalog | Vismaya Camp 2026',
  description: 'Discover the available workshops for your camper.',
};

export default function Catalog() {
  return <WorkshopCatalog />;
}
