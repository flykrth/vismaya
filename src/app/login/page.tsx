import { LoginForm } from '@/views/auth/LoginForm';

export const metadata = {
  title: 'Login | Vismaya Camp 2026',
  description: 'Sign in to manage your campers and registrations.',
};

export default function Login() {
  return <LoginForm />;
}
